#include <iostream>
#include <string>
#include <cmath>

int main()
{
    /*
    //for loop - loop that will execute a block of code a specified amount of time

    //for(int i = 1; i <= 10; i++) //for(int i = 1; i <= 10; i+2)
    //{
        //std::cout << "Happy new year!\n";
        //std::cout << i << '\n';
    ///}

    //std::cout << "Happy new year!\n";

    int k;
    int num;
    int average;
    int sum;

    std::cout << "Enter a number to be process: ";
    std::cin >> k;

    for(int i = 0; i < k; i++)
    {
        std::cout << "Enter number: ";
        std::cin >> num;
        sum = sum += num;
    }

    average = sum / k;

    std::cout << "The average is: " << average;
    */

    /*
    int fact = 5;
    int factorial = fact;

    for(int i = factorial - 1; i > 0; i--)
    {
        factorial = factorial * i;
    }

    std::cout << "Factorial of " << fact << ": " << factorial;

    return 0;
}
